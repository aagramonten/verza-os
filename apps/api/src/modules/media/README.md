# media module

Photo uploads via StorageProvider port; local adapter first (Day 6).

Owns its Prisma models per docs/vera-chat-mvp-plan.md; communicates with other modules only via application services.
